
public class PlayerComparetor {
	/* if(a.score>b.score){
         return -1;
     }else if(a.score<b.score){
         return 1;
     }else if(a.score==b.score){
        return  a.name.compareTo(a.name);

     }
     return 0;
*/
}
